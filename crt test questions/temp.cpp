#include <iostream>
using namespace std ; 
int main(){

    // temprature conversion 
   /// float temp ; 
    //cin >> temp ; 
    //float output = (9/5.0)*temp+32 ; 
    //cout << " the temprature in farenhite is  "<< output ; 




// counting the numbers 

/*
 int  amount ; 
 cin >> amount ; 
 int no_of_notes = 0; 
    no_of_notes= no_of_notes + amount/100; 
    amount = amount % 100 ; 
    no_of_notes= no_of_notes + amount/50; 
    amount = amount % 50; 
    no_of_notes= no_of_notes + amount/20;  
    amount = amount % 20 ;  
    no_of_notes = no_of_notes+ amount ; 
    cout << " the no of notes is "<< no_of_notes; 
   */

    



   // check the number is odd or even 

   /*  int a , b , c ; 
   cin >> a >> b >> c ; 
   int max ; 
   if ( a> b ){
    max = a ; 
   }
  else {
    max = b ; 

  }

  if (max > c ){
    max = max ; 
  }
  else {
    max = c ; 
  }
  cout << " the maximum of three of them is "<< max << endl; 
   */
  int num ; 

  cin >> num ;
  for (int i  =  1 ; i<=10; i++){
    cout<<"Num * " <<i*num<<" "<<endl;
  }



}